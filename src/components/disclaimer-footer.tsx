export function DisclaimerFooter() {
  return (
    <footer className="no-print border-t border-hairline bg-background/80 backdrop-blur px-6 py-3">
      <p className="text-[11px] leading-relaxed text-muted-foreground max-w-4xl">
        PharmaPrompt OS supports — it does not replace — pharmacist clinical judgement. All recommendations
        require pharmacist verification before counselling. Information reflects Australian community-pharmacy
        practice and may not capture every patient-specific factor.
      </p>
    </footer>
  );
}
